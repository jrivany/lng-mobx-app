{
  "name": "lng-mobx-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "samsung": "npm run build && ./install-tizen.sh"
  },
  "devDependencies": {
    "typescript": "^4.9.3",
    "vite": "^4.0.0"
  },
  "dependencies": {
    "@lightningjs/core": "^2.8.1",
    "mobx": "^6.7.0"
  }
}
